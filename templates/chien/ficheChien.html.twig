{% extends 'base.html.twig' %}

{% block title %}Fiche chien{% endblock %}

{% block body %}
<h1 class="pt-2 text-center"> <a href=" {{path('ficheChien', {'id': leChien.id})}} " class="badge badge-success"> {{leChien.nom}} </a></h1>
<hr>
                <div class="card-body">
                    <th scope="col-4">Description</th>
                    <p class="card-text">{{leChien.description|raw}}</p>
                </div>
    </div>
        <table class="table table-hover mx-auto">
        <thead>
            <tr>
            <th scope="col-4">Prix : {{leChien.prix}} $</th>
            
            </tr>
        </thead>
    </table>
</div>
{% endblock %}