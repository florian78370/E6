{% extends 'base.html.twig' %}

{% block title %}ログイン{% endblock %}

{% block body %}
<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
        <div class="mb-3">
            Vous êtes connecter sous le nom {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Se connecter</a>
        </div>
    {% endif %}

    <h1 class="h3 mb-3 font-weight-normal">サインインしてください</h1>
    <label for="inputEmail">電子メール</label>
    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
    <label for="inputPassword">パスワード</label>
    <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>

    <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}"
    >

    <button class="btn btn-lg btn-primary" type="submit">
        ログイン
    </button>
</form>
<p><a href=" {{path('app_forgot_password_request')}}">Mots de passe oublié</a></p>
{% endblock %}
